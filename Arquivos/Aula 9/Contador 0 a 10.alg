algoritmo "Contador"
var
   contador : inteiro
inicio
      Contador <- 0
      Enquanto (contador <= 10) faca
               EscrevaL("Contagem:", contador)
               Contador <- contador + 1
      FimEnquanto
fimalgoritmo
